# AGENTS.md

## 1. 项目定位

`cocos-fbx-clip-slicer` 用于 Cocos Creator 3.8.x 项目中的 FBX 动画切片管理。

动画设计师通过 FBX 文件名提供：

- 美术概念名，通常为中文；
- 起始帧；
- 结束帧。

插件负责在 Cocos Creator 面板中维护并写入：

- 目标 Clip 名，通常为游戏代码使用的英文名称；
- 播放模式；
- 播放速度；
- 用户手动调整后的帧范围、顺序及切片增删结果。

项目当前的真实使用规模通常是每次处理 1～3 个 FBX。不要将批处理作为近期重点，也不要擅自把产品方向扩展为目录扫描工具。

---

## 2. 当前第一阶段目标

实现 Cocos Creator 3.8.x 编辑器扩展，提供完整的单个 FBX 动画切片面板。

核心用户路径：

1. 用户在资源管理器中右键一个 FBX；
2. 在右键菜单中选择“在 FBX Clip Slicer 中打开”；
3. 插件打开或聚焦唯一的主面板；
4. 面板读取当前 FBX `.meta` 中已有的一个或多个 Split；
5. 表格原样展示当前分割结果；
6. 用户可以手动增删改、调整顺序；
7. 用户可以点击“重新解析文件名”，从 FBX 文件名提取美术概念名及帧范围；
8. 插件将解析结果与当前表格、项目预设和该 FBX 的历史状态合并；
9. 用户确认后应用到当前 FBX，并触发重新导入。

打开面板本身必须是只读动作：不得自动解析、不得自动写 Meta、不得自动重新导入。

---

## 3. 第一版明确不做的内容

除非用户后续明确调整范围，否则第一版不实现：

- 目录级批量处理；
- 自动扫描整个项目；
- AI 自动翻译中文动画名称；
- 复杂的旧 Split 子资源引用稳定算法；
- 自动替换外部新版 FBX；
- 多 Take FBX 的完整编辑支持。

“导入并替换 FBX”是后续功能。第一版可以预留入口和服务接口，但不得为了该功能拖慢当前面板交付。

---

## 4. 统一术语

界面、类型、文档和日志优先使用以下术语：

| 术语 | 定义 |
|---|---|
| 美术概念名 | 动画设计师写在 FBX 文件名中的动画名称，通常为中文，也是未来资源更新时的主要对应键 |
| 目标 Clip 名 | 最终写入 Cocos Split，并由游戏代码使用的名称，通常为英文 |
| 起始帧 | 当前切片的开始帧 |
| 结束帧 | 当前切片的结束帧 |
| 播放模式 | Default、Normal、Loop、PingPong、Reverse、LoopReverse、PingPongReverse 等模式 |
| 速度 | 写入 Split 的播放速度 |
| 当前 Meta Split | 当前 FBX `.meta` 文件中真实存在的分割信息 |
| 项目 Clip 预设 | 项目级持久保存的“美术概念名 → 目标名称、播放模式、速度”规则 |
| FBX 资源状态 | 按 FBX 根 UUID 持久保存的美术概念名、上次解析值及表格对应关系 |

不要继续使用含义过窄的“中文标签 → 英文名映射”描述。该配置是完整的动画 Clip 预设。

---

## 5. 面板入口与打开行为

### 5.1 资源右键入口

仅对 FBX 资源显示：

```text
FBX Clip Slicer
└─ 在 Slicer 面板中打开
```

点击后：

- 打开或聚焦单例主面板；
- 将该 FBX 设置为当前目标；
- 读取 FBX 资源信息及 `.meta`；
- 加载项目 Clip 预设；
- 加载按根 UUID 保存的 FBX 资源状态；
- 展示当前 Meta 中已有的所有 Split。

### 5.2 现有 Split 的展示

原 FBX 可能具有：

- 一个默认整段动画；
- 一个已经分割好的 Clip；
- 多个已经分割好的 Clip；
- 没有可用 Split。

面板必须忠实展示当前 Meta，不得假设资源尚未分割。

当前 Meta 本身通常不包含“美术概念名”。若无法从 FBX 资源状态恢复，对应单元格可以为空或显示“未关联”，但必须允许用户编辑。

---

## 6. 主表格设计

建议列：

| 列 | 可编辑 | 说明 |
|---|---:|---|
| 顺序 | 是 | 支持拖拽或上移、下移 |
| 美术概念名 | 是 | 默认来自文件名；允许用户修改，不建议但不禁止 |
| 目标 Clip 名 | 是 | 最终写入 Split 的名称 |
| 起始帧 | 是 | 数字输入 |
| 结束帧 | 是 | 数字输入 |
| 播放模式 | 是 | 使用符号枚举下拉框，不在 UI 中暴露魔法数字 |
| 速度 | 是 | 数字输入，默认 1 |
| 状态 | 否 | 当前、已修改、新增、待删除、未配置、冲突等 |
| 操作 | 是 | 删除、复制、上移、下移等 |

### 6.1 手动分割能力

表格必须支持：

- 新增行；
- 删除行；
- 修改任意字段；
- 复制现有行；
- 调整顺序；
- 撤销待删除状态或在应用前恢复；
- 从当前 Meta 重新加载。

用户对表格有最终决策权。插件可以警告，但不要因为字段来自文件名就禁止编辑。

### 6.2 帧显示和转换

Meta 中 `from`、`to` 通常为秒，面板使用帧：

```text
frame = time * fps
```

要求：

- 使用 epsilon 处理浮点误差；
- 接近整数时显示整数；
- 若已有 Meta 值无法稳定还原为整数帧，允许显示小数并提示；
- 写入时统一通过当前 FBX 的 FPS 转换为秒；
- 不要在多个模块中重复实现转换逻辑。

---

## 7. 播放属性

项目预设至少保存：

```ts
interface ClipPreset {
  targetClipName: string;
  wrapMode: WrapModeName;
  speed: number;
}
```

符号类型建议为：

```ts
type WrapModeName =
  | 'Default'
  | 'Normal'
  | 'Loop'
  | 'PingPong'
  | 'Reverse'
  | 'LoopReverse'
  | 'PingPongReverse';
```

要求：

- UI 和项目配置只保存符号名称；
- Cocos Meta 数值转换集中在一个适配模块；
- 不要在 UI、业务逻辑和测试中散落魔法数字；
- 实现前必须用 Cocos Creator 3.8.x 实际 Meta 或官方 API 验证各模式数值；
- 速度必须为有限数字；
- 默认速度为 `1`；
- 速度为 `0`、`NaN`、`Infinity` 时禁止应用；
- 是否允许负速度必须由明确产品规则决定，默认不使用负速度表达反向播放，应优先使用 Reverse 类播放模式。

---

## 8. 两层项目持久化

仅保存项目级 Clip 预设不足以恢复已有 FBX 的“美术概念名”。因此必须区分两层数据。

### 8.1 项目 Clip 预设

按美术概念名保存全项目通用规则，不保存帧范围：

```ts
interface ProjectClipPreset {
  targetClipName: string;
  wrapMode: WrapModeName;
  speed: number;
}

type ProjectClipPresetMap = Record<string, ProjectClipPreset>;
```

示例：

```json
{
  "正常待机": {
    "targetClipName": "idle",
    "wrapMode": "Loop",
    "speed": 1
  },
  "正常移动": {
    "targetClipName": "move",
    "wrapMode": "Loop",
    "speed": 1
  }
}
```

### 8.2 FBX 资源状态

按 FBX 根 UUID 保存。不要使用文件路径作为唯一键，因为未来资源可能重命名，但 UUID 保持不变。

建议结构：

```ts
interface AssetClipState {
  rowId: string;
  artConceptName: string;
  targetClipName: string;
  fromFrame: number;
  toFrame: number;
  wrapMode: WrapModeName;
  speed: number;
  order: number;
  lastParsedArtConceptName?: string;
  lastParsedFromFrame?: number;
  lastParsedToFrame?: number;
}

interface FbxAssetState {
  assetUuid: string;
  clips: AssetClipState[];
  updatedAt: string;
  schemaVersion: number;
}
```

用途：

- 下次打开 FBX 时恢复美术概念名；
- 判断用户是否在上次解析后手动修改过名称或帧范围；
- 未来替换新 FBX 时按美术概念名进行对应；
- 即使资源文件名变化，只要根 UUID 不变仍可恢复对应关系。

### 8.3 存储要求

- 使用 Cocos 扩展的项目级配置能力；
- 配置跟随项目保存，适合加入版本控制；
- 配置结构必须带 `schemaVersion`；
- 读取时兼容缺失字段并补默认值；
- 写入应集中由配置服务完成；
- 不允许面板组件直接散写配置文件。

---

## 9. “重新解析文件名”的精确定义

“重新解析文件名”只更新面板内存状态，不直接写 Meta，不直接保存项目配置，不触发重新导入。

FBX 文件名只提供：

- 美术概念名；
- 起始帧；
- 结束帧。

目标 Clip 名、播放模式、速度来自当前表格、项目预设或默认值。

### 9.1 合并优先级

重新解析时，属性来源优先级：

```text
1. 当前面板中用户尚未应用的编辑
2. 当前 FBX 资源状态
3. 项目 Clip 预设
4. 当前 Meta Split
5. 插件默认值
```

### 9.2 对应规则

首选以规范化后的“美术概念名”精确匹配。

规范化只能处理无语义差异内容，例如：

- 首尾空白；
- 全角、半角空格；
- 明确约定的标点差异。

不要默认使用模糊匹配、拼音相似或 AI 推断自动覆盖用户数据。

### 9.3 匹配成功后的字段处理

若解析出的美术概念名与现有行匹配：

- 保留当前行的目标 Clip 名；
- 保留当前行的播放模式；
- 保留当前行的速度；
- 美术概念名若已由用户手动修改，默认保留，并显示来源差异；
- 起止帧是否更新取决于脏状态。

帧范围规则：

- 当前帧范围仍等于 `lastParsedFromFrame/lastParsedToFrame`：说明用户未手改，可更新为本次解析值；
- 当前帧范围与上次解析值不同：说明用户可能手改，默认保留当前值，并标记“文件名范围有变化”；
- 用户可以在冲突提示中选择“采用文件名范围”或“保留手动范围”。

这可以避免重复点击“重新解析文件名”时覆盖已经手动调整的数据，同时又能识别动画设计师真正修改了帧范围。

### 9.4 新增项

解析到新的美术概念名时：

1. 先查项目 Clip 预设；
2. 有预设则自动填入目标名称、播放模式和速度；
3. 无预设则目标 Clip 名留空，播放模式为 Default，速度为 1；
4. 标记为“未配置”或“新增”。

目标 Clip 名为空时禁止最终应用。

### 9.5 缺失项

当前表格或 Meta 中存在，但本次文件名未解析到的行：

- 不要立即消失；
- 标记为“文件名中缺失”或“待删除”；
- 由用户决定保留或删除；
- 只有点击应用后才修改 Meta。

### 9.6 解析失败

文件名解析失败时：

- 保留当前表格全部内容；
- 显示明确错误位置和原因；
- 不清空数据；
- 不写任何文件。

---

## 10. 项目预设编辑规则

用户修改表格时，可能只是当前 FBX 的特殊需求，也可能希望更新全项目预设。

每一行应预留：

```ts
saveToProjectPreset: boolean;
```

默认行为可以是：

- 新美术概念名：默认勾选保存项目预设；
- 已有项目预设且值未变化：无需重复提示；
- 已有项目预设但当前值不同：显示差异，让用户选择仅本次使用或更新项目预设。

帧范围和顺序不得保存进项目 Clip 预设；它们只属于当前 FBX 资源状态。

---

## 11. 应用到 FBX

建议按钮文案：

```text
应用到 FBX 并重新导入
```

执行顺序：

1. 校验当前表格；
2. 生成待写入 Split；
3. 显示或计算变更摘要；
4. 保存需要更新的项目 Clip 预设；
5. 备份当前 `.fbx.meta`；
6. 仅更新 `userData.animationImportSettings[].splits`；
7. 保留根 UUID 和所有非 Split 导入配置；
8. 触发 Cocos 重新导入；
9. 重新读取 Meta 验证结果；
10. 保存按根 UUID 记录的 FBX 资源状态；
11. 刷新表格并清除脏状态。

应用失败时：

- 不得留下部分写入状态；
- 尽可能恢复 Meta 备份；
- 不更新 FBX 资源状态；
- 给出可操作的错误信息。

### 11.1 校验要求

至少校验：

- 目标 Clip 名不能为空；
- 同一个 FBX 内目标 Clip 名不得重复；
- 美术概念名可为空但必须警告，因为未来资源对应会受影响；
- 起止帧必须是有限数字；
- 结束帧不得小于起始帧；
- 帧范围不得重叠，除非未来明确允许；
- 帧范围不得明显超出 FBX 动画总时长；
- 播放模式必须是受支持的符号枚举；
- 速度必须有效；
- 单 Take 限制需要明确提示。

### 11.2 Split 内部标识

当前产品场景通常整体使用 FBX 上的 `SkeletalAnimation`，动画 Clip 不被其他资源单独引用。

因此：

- 不要把复杂的旧 Split `previousId` 对账作为核心目标；
- 当前行对应明确时可以保留原 ID；
- 新增行生成合法 ID；
- 删除行移除对应 Split；
- 不承诺单个 AnimationClip 子资源引用在任意重建后保持稳定；
- 根 FBX UUID 和非动画导入配置必须保持稳定。

---

## 12. 面板状态与用户体验

面板至少维护以下状态：

```ts
interface ClipEditorRow {
  rowId: string;
  previousId?: string;
  artConceptName: string;
  targetClipName: string;
  fromFrame: number;
  toFrame: number;
  wrapMode: WrapModeName;
  speed: number;
  order: number;
  status: 'current' | 'modified' | 'added' | 'pending-delete' | 'unconfigured' | 'conflict';
  saveToProjectPreset: boolean;
  dirtyFields: Set<EditableClipField>;
  lastParsed?: {
    artConceptName: string;
    fromFrame: number;
    toFrame: number;
  };
}
```

要求：

- 切换目标 FBX 前，若当前表格有未应用修改，应提示；
- “从当前 Meta 重新加载”会丢弃面板未应用修改，必须确认；
- “重新解析文件名”不得静默清空或覆盖脏字段；
- 操作完成后显示清晰摘要；
- 错误必须定位到具体行和字段；
- 表格顺序即最终 Split 顺序。

---

## 13. 未来功能：导入并替换 FBX

面板未来增加：

```text
导入并替换 FBX……
```

目标：用动画设计师提供的新 FBX 二进制内容替换当前模型，同时保留当前项目资源身份。

预期流程：

1. 当前面板已有目标 FBX；
2. 用户选择外部新 FBX；
3. 从外部文件名解析美术概念名及帧范围；
4. 使用当前 FBX 资源状态和项目 Clip 预设合并；
5. 在面板中预览并允许人工修改；
6. 备份原 FBX 和原 Meta；
7. 替换 FBX 文件内容；
8. 不用新 Meta 覆盖旧 Meta；
9. 保留旧 Meta 根 UUID 和非动画导入配置；
10. 更新旧 Meta 的 Split；
11. 重新导入并验证；
12. 失败时同时恢复 FBX 和 Meta。

关于文件名：

- 资源引用主要依赖 Meta UUID；
- 未来可以支持保留项目内旧文件名，或改用新 FBX 的长中文名；
- 若项目存在基于字符串路径的动态加载，重命名可能破坏路径；
- 因此默认应优先保留项目内现有文件名，重命名必须明确提示。

第一版只需保证数据模型和服务边界不妨碍该功能，不需要立即实现。

---

## 14. 推荐架构

核心逻辑必须与 Cocos UI 解耦，保持可测试。

建议结构：

```text
src/
  parser.ts                 文件名解析
  types.ts                  共享类型
  wrap-mode.ts              符号模式与 Cocos 数值转换
  split-converter.ts        帧与秒转换、Meta Split 转表格行
  merge.ts                  重新解析时的合并和冲突判断
  validation.ts             表格校验
  meta.ts                   安全更新 Meta
  config.ts                 CLI 配置兼容层

extensions/cocos-fbx-clip-slicer/
  package.json
  source/
    main.ts
    services/
      asset-service.ts      获取目标 FBX、Meta、UUID、重新导入
      project-profile.ts    项目预设与 FBX 资源状态
      apply-service.ts      应用、备份、回滚、验证
    panels/default/
      index.ts
      index.html
      index.css
      model.ts              面板状态和命令
      components/           表格、模式选择、冲突提示
```

原则：

- `src/` 不依赖 `Editor` 全局；
- Cocos API 只存在于扩展适配层；
- 解析、合并、校验、转换都必须可由 Node 测试直接调用；
- 面板只负责交互，不直接修改文件；
- 所有写入通过服务层；
- CLI 可以继续使用共享核心，但不是当前优先方向。

---

## 15. 测试要求

新增功能必须覆盖以下测试场景。

### 15.1 当前 Meta 读取

- 一个默认 Split；
- 多个已有 Split；
- 无 Split；
- FPS 浮点换算；
- 非整数帧还原提示；
- 非 FBX Meta；
- 多 Take 明确报错。

### 15.2 手动编辑

- 新增行；
- 删除行；
- 恢复待删除行；
- 修改美术概念名；
- 修改目标 Clip 名；
- 修改起止帧；
- 修改播放模式和速度；
- 复制行；
- 调整顺序。

### 15.3 重新解析合并

- 第一次解析；
- 重复解析同一文件名，不覆盖已有配置；
- 项目预设自动填充；
- 相同美术概念名但帧范围变化；
- 用户手改帧范围后再次解析；
- 新增美术概念名；
- 文件名中删除已有概念；
- 当前面板有未应用修改；
- 解析失败时表格保持不变。

### 15.4 持久化

- 保存和恢复项目 Clip 预设；
- 保存和恢复 FBX 资源状态；
- FBX 路径变化但根 UUID 不变时仍可恢复；
- 配置缺失字段自动迁移；
- schemaVersion 升级。

### 15.5 应用和安全

- 只修改 Split；
- 根 UUID 保持不变；
- 非动画导入配置保持不变；
- 无变化时不产生无意义写入；
- 写入失败能够恢复；
- 应用后重新读取结果一致；
- 重名、空名称、非法速度、重叠范围被阻止。

---

## 16. 开发顺序

按以下顺序推进，除非用户明确调整：

### 阶段 A：扩展骨架与只读面板

- Cocos Creator 3.8.x 扩展工程；
- FBX 右键菜单；
- 单例主面板；
- 读取当前 FBX Meta；
- 展示一个或多个现有 Split。

### 阶段 B：手动分割编辑器

- 表格全字段编辑；
- 增删、复制、删除恢复；
- 顺序调整；
- 脏状态；
- 校验。

### 阶段 C：文件名重新解析和合并

- 解析文件名；
- 项目 Clip 预设；
- FBX 资源状态；
- 安全合并和冲突提示。

### 阶段 D：应用与重新导入

- Meta 备份；
- 写入 Split；
- 重新导入；
- 验证和回滚；
- 状态持久化。

### 阶段 E：未来替换资源

- 外部 FBX 选择；
- 文件内容替换；
- UUID 和旧 Meta 保留；
- 完整事务和回滚。

---

## 17. 第一版验收标准

第一版完成时，必须满足：

1. 右键任意项目内 FBX 可以在主面板中打开；
2. 一个或多个现有 Split 能正确显示；
3. 所有表格字段均可编辑；
4. 可以增删、复制和调整顺序；
5. 点击“重新解析文件名”后，正确提取美术概念名和帧范围；
6. 重复解析不会静默覆盖用户已修改属性；
7. 项目预设可以保存目标 Clip 名、播放模式和速度；
8. 美术概念名及当前资源对应关系可以按 FBX 根 UUID 恢复；
9. 新概念名能自动使用已有项目预设；
10. 应用时只更新 Split，不改变根 UUID 和其他导入设置；
11. 应用后 Cocos 自动重新导入；
12. 重新打开项目后配置仍然存在；
13. 错误不会清空表格或损坏 Meta；
14. 核心解析、合并、校验和 Meta 更新具有自动化测试。

---

## 18. 对后续开发代理的约束

处理本项目任务时必须遵守：

- 先阅读本文件，再修改实现；
- 优先解决 Cocos 插件主面板，不要擅自转向批处理；
- 不得把打开面板变成自动写入行为；
- 不得禁止用户编辑美术概念名或帧范围；
- 不得在重新解析时静默覆盖脏字段；
- 不得直接覆盖整个 Meta；
- 不得改变 FBX 根 UUID；
- 不得把帧范围保存为项目通用 Clip 预设；
- 必须按根 UUID 保存 FBX 资源状态；
- 不得在 UI 中使用 Cocos WrapMode 魔法数字；
- 不得让 UI 组件直接操作文件系统或项目配置；
- 新增行为必须补测试；
- 对 Cocos 3.8.x API 不确定时，先验证实际 API 和 Meta，再实现；
- 对需求存在歧义时，优先遵循本文的用户工作流和数据所有权原则。

数据所有权原则：

```text
文件名：提供美术概念名、起始帧、结束帧
项目预设：提供目标 Clip 名、播放模式、速度
当前 FBX 资源状态：保存概念对应、帧范围、顺序和上次解析信息
用户表格编辑：拥有最终决策权
Meta：保存 Cocos 最终导入结果和资源身份
```
